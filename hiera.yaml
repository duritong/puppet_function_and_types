---
version: 5
hierarchy:

:hierarchy:
  - name: hiera
    data_hash: yaml_data
    datadir: hieradata
    paths:
      - hosts/%{facts.fqdn}.yaml
      - defaults.yaml


defaults:
  data_hash: yaml_data
  datadir: hieradata

